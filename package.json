{
  "name": "adminapi",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "target": "ES2020",
  "module": "CommonJS",
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "build": "npm run monorepo:build:release",
    "clean": "node _build/scripts/clean.js",
    "monorepo:build:release":  "npm run monorepo:clean && npm run build:release -ws && npm run monorepo:bundle",
    "monorepo:clean":  "npm run clean && npm run clean -ws",
    "monorepo:bundle": "node _build/scripts/bundle.js"
  }
}